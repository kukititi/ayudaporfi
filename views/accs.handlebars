<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda Cool</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/css/estiloproductos.css">
</head>
{{> navbar}}
            <section class="col-md-9">
                <h3>ACCESORIOS</h3>
                <img src="/Imagenes/BANNER ACCESORIOS.png" alt="Accesorios" class="img-fluid mb-4">

{{#each lista}}
                <div class="row">
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="{{this.foto}}" class="card-img-top" alt="SET MOTOR GRIP C/TAPA M4 BK">
                            <div class="card-body">
                                <h5 class="card-title">{{this.name}}</h5>
                                <p class="card-text">${{this.price}}</p>
                                <button class="alert-button">agregar al carrito</button>
                                <div class="card__description">{{this.descr}}</div>
                            </div>
                        </div>
                    </div>
                </div>

{{/each}}


{{> footer}}