<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil - Tienda Cool</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/profile.css">
</head>
<header>
    {{> navbar}}
</header>
<body>
<div class="container wallet-container">
    <div class="card">
        <div class="card-header">
            <h5>Perfil del Usuario</h5>
        </div>
        <div class="card-body d-flex align-items-center">
            <div class="profile-image-container me-3">
                <img src="/Imagenes/fotoperfil.jpg" alt="Imagen de Perfil" class="img-fluid mb-4">
            </div>
            <div>
                <div class="virtual-card mb-3">
                    <div class="virtual-card-header text-center mb-2">
                        <h6>Tarjeta Virtual</h6>
                    </div>
                    <div class="virtual-card-body">
                        <p><strong>Número de Tarjeta:</strong> 4496 2934 9012 3484</p>
                        <p><strong>Nombre del Titular:</strong> {{user.name}}</p>
                        <p><strong>Fecha de Vencimiento:</strong> 08/34</p>
                    </div>
                    <div class="balance-container mt-3">
                        <p><strong>Email:</strong> {{user.email}}</p>
                        <p><strong>Saldo:</strong> ${{user.wallet}}</p>
                        <button class="btn btn-recharge" data-bs-toggle="modal" data-bs-target="#rechargeModal">Recargar Saldo</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de Recarga -->
<div class="modal fade" id="rechargeModal" tabindex="-1" aria-labelledby="rechargeModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="rechargeModalLabel">Recargar Saldo</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <label for="rechargeAmount" class="form-label">Ingrese la cantidad a recargar:</label>
                <input type="number" id="rechargeAmount" class="form-control" placeholder="Monto en CLP" min="1">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <button type="button" class="btn btn-primary" id="confirmRecharge">Confirmar</button>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/script.js"></script>
</body>
</html>
